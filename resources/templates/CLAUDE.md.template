# 工作区 Claude 规则

本文件由 `claude-project-init` 初始化。它用于说明 Claude Code 在当前工作区中的上下文读取、技能调用、索引维护和完成检查规则。

默认使用中文沟通。低风险本地读写和整理任务可以在用户授权范围内直接执行；涉及删除、覆盖、发布、推送、外部服务或高权限配置时先确认。

## 上下文读取顺序

1. 需要了解工作区资料时，先读 `.claude/workspace-index.md`。
2. `.claude/workspace-index.md` 只记录入口级索引，不放长篇规则和历史内容。
3. 只读取索引中与当前任务相关的文件；不要为了“熟悉项目”全量遍历仓库。
4. 如果任务落在子项目内，再读取该子项目自己的 `CLAUDE.md`、`README.md` 或 `.claude` 规则。
5. 如果索引缺失或过期，先做最小范围搜索，并在需要时更新索引；不要扩大到全仓扫描。

## 工作区技能使用

- 所有工作区技能统一放在 `.claude/skills/` 下。
- `.claude/skills/<skill-name>/SKILL.md` 是技能入口。
- 技能配套资料、模板、检查清单、hooks 等放在该技能自己的 `resources/` 下。
- 技能在当前工作区生成或维护的长期数据，优先放在 `local/<skill-name>/` 下。
- 使用技能前优先读取 `.claude/skills/INDEX.md` 定位，不全量遍历所有技能资料。

<!-- claude-project-init:skills:start -->
<!-- claude-project-init 会在这里维护已安装 workspace skills 列表。 -->
<!-- claude-project-init:skills:end -->

## 工作日志规则

如果安装了 `work-journal`：

- 当用户提出需要跨轮推进的新需求、继续历史需求、要求记录沟通内容、要求复盘或要求检查完成情况时，优先使用 `.claude/skills/work-journal/SKILL.md`。
- 工作记录默认落在 `local/work-journal/records/`，总索引在 `local/work-journal/index.md`。
- 历史记录必须先读 `local/work-journal/index.md`，根据标题、关键词、状态、备注定位相关记录，只读取命中的记录文件。
- 新需求默认新建记录；只有用户明确说继续历史事项，或索引显示强相关时，才追加到已有记录。
- 用户明确要求“只读检查/只给建议/不要修改文件/不要落盘”时，不创建或更新工作记录。

## 思考辅助规则

如果安装了 `clear-thinking`：

- 当需求复杂，涉及多目标冲突、产品/研发/经营/组织/长期选择，或用户说“想清楚/分析/判断/规划/复盘”时，可使用 `.claude/skills/clear-thinking/SKILL.md`。
- `clear-thinking` 只辅助判断真实目标、主要矛盾、约束、最小行动和验证方式，不替代工作日志。
- 使用时保持低噪声，只读取与当前判断相关的最小材料。

## 大任务与子代理编排

- 如果任务跨仓库、跨项目、跨阶段、多材料、多轮实现与验证，或单个对话难以稳定协调，优先使用 Workflow 或多个子代理并行管理流程。
- 可独立搜索、审查、验证的任务优先拆给多个子代理并行处理，并在主会话汇总。
- 简单单点修改、一次性短答、强上下文耦合且无法拆分的任务，不强行升级。

## 调度决策表

| 场景 | 默认处理方式 |
| --- | --- |
| 单点明确、低风险、无需跨轮追踪 | 主会话直接处理 |
| 纯 Git 提交/推送/拉取/同步 | 主会话直接处理；除非用户要求记录，否则不创建工作记录 |
| 需要记录、续接、复盘或完成检查 | 使用 `work-journal` 定位并判断是否创建/更新记录 |
| 目标不清、多目标冲突、需要取舍判断 | 使用 `clear-thinking` 输出短结论；需要多材料时交给子代理 |
| 可拆成多个独立专项且并行收益明显 | 多个子代理并行，主会话汇总 |
| 跨项目/跨阶段/多材料/多轮执行验证 | Workflow 编排 |
| 用户明确只读审查、只给建议、不修改文件或不落盘 | 只读检查并回复结论 |

## 工作区索引维护

- `.claude/workspace-index.md` 是入口级导航索引，不是完整文件树。
- 后续新增、删除、移动、重命名长期资料入口时，应同步更新 `.claude/workspace-index.md`。
- 发现某目录下更权威的 `README.md`、`INDEX.md`、`CLAUDE.md` 时，也应更新索引。
- 临时产物、一次性脚本输出、普通中间文件不写入索引，避免索引变重。
- 历史工作记录的具体路径和描述不直接写进 `.claude/workspace-index.md`，而是写进对应二级索引，例如 `local/work-journal/index.md`。

## 完成声明规则

在回复“完成/已做好”前，至少确认：

1. 用户本轮要做的事项是否都覆盖。
2. 文件修改或产物路径是否明确。
3. 能验证的内容是否已验证；不能验证的原因是否说明。
4. 是否需要更新 `.claude/workspace-index.md`；如果需要，是否已经更新。
5. 是否需要读取历史工作记录；如果需要，是否只通过索引定位并按需读取。
6. 未完成、待确认、风险点是否写清。

不要把“已修改但未验证”说成“已完成”。
