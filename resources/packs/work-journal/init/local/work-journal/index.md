# 工作日志索引

> 由 `work-journal` 技能维护。新记录默认放在 `local/work-journal/records/`。

## 使用规则

- 先通过本索引定位历史记录，再按需读取命中的记录文件。
- 不要默认全量读取 `records/`。
- 新需求默认新建记录；只有用户明确要求继续历史事项，或本索引显示强相关时，才追加到已有记录。

## 记录列表

| 日期 | 标题 | 状态 | 关键词/适用场景 | 记录文件 | 备注 |
| --- | --- | --- | --- | --- | --- |
